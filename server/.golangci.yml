version: "2"
linters:
  default: standard
  enable:
    - gosec
    - bodyclose
  exclusions:
    rules:
      - linters: [gosec]
        text: "G101:"
      - linters: [gosec]
        path: _test\.go
